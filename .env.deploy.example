# Optional root-level deploy overrides.
# Copy to .env.deploy.uat.local or .env.deploy.production.local.
# The .local files are ignored by git.

# Shared remote target (current UAT defaults to OCI)
DEPLOY_HOST=161.118.161.98
DEPLOY_USER=ubuntu
DEPLOY_PORT=22

# Collage service
COLLAGE_SERVICE_API_KEY=
COLLAGE_WORKER_POLL_INTERVAL_SECONDS=10
COLLAGE_WORKER_BATCH_SIZE=5
